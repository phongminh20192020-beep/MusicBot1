# One proxy per line. Supported schemes: http://, https://, socks4://, socks5://
# Credentials go in the URL: http://user:pass@host:port
# Lines starting with # and blank lines are ignored.
#
# Copy this file to proxies.txt (already gitignored) and fill in real proxies.
# /mv download tries a direct connection first, then works through this list
# in order any time YouTube responds with a bot-block/rate-limit error.
#
# http://user:pass@1.2.3.4:8080
# socks5://5.6.7.8:1080
